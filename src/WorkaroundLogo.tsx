import { Typography } from "@mui/material"
import React from "react"

interface Props {}

const WorkaroundLogo = (props: Props) => {
   return <div className="text-orange">workaround</div>
}

export default WorkaroundLogo
